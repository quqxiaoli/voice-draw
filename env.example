# 复制为 .env 后填入真实值。.env 已在 .gitignore,绝不提交。
# 缺任一变量时后端启动应直接报错并提示来这里配。

# 产品运行时 LLM(国内服务器可达,赛前已验证)
DEEPSEEK_API_KEY=

# 服务端口
PORT=8080

# 前端访问后端的地址(部署时改为服务器公网地址,如 http://1.2.3.4:8080)
NEXT_PUBLIC_API_BASE=http://localhost:8080

# 议题需要才启用 ↓
# ASR_API_KEY=
